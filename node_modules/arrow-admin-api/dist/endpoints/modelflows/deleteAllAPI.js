'use strict';

var _require = require('./util'),
    getShortName = _require.getShortName;

var deleteAllAPI = function deleteAllAPI(name, model) {
	return {
		schemaVersion: '2',
		name: name,
		start: 'model.deleteall',
		nodes: {
			'model.deleteall': {
				type: 'nodehandler://arrow-flow-invoke/model/' + model.name,
				method: 'deleteAll',
				name: 'Delete all ' + getShortName(model, true),
				parameters: [],
				outputs: {
					next: {
						routes: ['response.success']
					}
				}
			},
			'response.success': {
				type: 'nodehandler://axway-flow/http',
				name: 'Set response',
				method: 'setresponse',
				parameters: [{
					name: 'status',
					value: '204',
					type: 'number'
				}],
				outputs: {
					next: {
						context: '$.response'
					}
				}
			}
		}
	};
};

exports = module.exports = deleteAllAPI;