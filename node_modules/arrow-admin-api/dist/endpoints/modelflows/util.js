'use strict';

var pluralize = require('pluralize');

function getShortName(model, multiple) {
	var name = model.name.replace(/^.*?\//, '');
	return multiple ? pluralize(name) : name;
}

exports = module.exports = {
	getShortName: getShortName
};