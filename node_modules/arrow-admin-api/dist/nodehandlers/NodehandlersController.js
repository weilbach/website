'use strict';

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var BadRequestError = require('../errors/BadRequestError');
var NotFoundError = require('../errors/NotFoundError');

/**
 * The controller implementing the actions for interacting with node handlers.
 * @public
 */

var NodehandlersController = function () {
	/**
     * Create a NodehandlersController.
     * @param {object} ctx - The context for this controller.
     * @param {express} ctx.app - The express application.
     * @param {string} ctx.prefix - The prefix bound to.
     * @param {Arrow} ctx.arrow - The Arrow server.
     * @param {boolean} ctx.devmode - Flag indicating if this is a devmode server.
     * @param {Swagger} ctx.swagger - openapi-doc instance.
     */
	function NodehandlersController(ctx) {
		_classCallCheck(this, NodehandlersController);

		this.ctx = ctx;
		this.logger = this.ctx.arrow.logger;
	}

	_createClass(NodehandlersController, [{
		key: 'getFlowNodeSpec',
		value: function getFlowNodeSpec(specType) {
			var _this = this;

			if (!specType) {
				return Promise.reject(new BadRequestError());
			}
			return new Promise(function (resolve, reject) {
				var spec = void 0;
				try {
					spec = _this.ctx.arrow.getRegisteredNodeSpec(specType);
				} catch (e) {
					return reject(new BadRequestError(e.message));
				}
				if (!spec) {
					return reject(new NotFoundError());
				}
				return resolve(spec);
			});
		}
	}, {
		key: 'getFlowNodeSpecs',
		value: function getFlowNodeSpecs() {
			var _this2 = this;

			return new Promise(function (resolve, reject) {
				var specs = _this2.ctx.arrow.getRegisteredNodeSpecs();
				if (!specs) {
					return reject(new BadRequestError());
				}
				return resolve(specs);
			});
		}
	}]);

	return NodehandlersController;
}();

exports = module.exports = NodehandlersController;