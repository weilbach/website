'use strict';

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var APIProvider = require('../APIProvider');

/**
 * API for interacting with the Axway Schema repository.
 * @public
 */

var SchemaAPI = function (_APIProvider) {
	_inherits(SchemaAPI, _APIProvider);

	function SchemaAPI() {
		_classCallCheck(this, SchemaAPI);

		return _possibleConstructorReturn(this, (SchemaAPI.__proto__ || Object.getPrototypeOf(SchemaAPI)).apply(this, arguments));
	}

	_createClass(SchemaAPI, null, [{
		key: 'bind',

		/**
   * Bind the paths to the controller methods.
   *
   * @param {SchemaController} controller - The main controller providing the
   *                                         implementation for this API.
   */
		value: function bind(controller) {
			var swagger = controller.ctx.swagger;

			swagger.schema('Schema', { $ref: '#' });

			swagger.get('/schema').operationId('getSchemas').tag('schema').summary('Gets all the registered schemas.').response(200, 'OK', 'Schema', true).response(500).action(function (req, res) {
				controller.getSchemas().then(function (schemas) {
					res.status(200).send(schemas);
				}).catch(function (err) {
					APIProvider.handleError(err, res, 500);
				});
			});

			swagger.get('/schema/:id').operationId('getSchemaById').tag('schema').summary('Gets the schema by id.').parameter({ in: 'path', name: 'id', type: 'string', description: 'The schema id.' }).response(200, 'OK', 'Schema').response(404).response(500).action(function (req, res) {
				var id = decodeURIComponent(req.params.id);
				controller.getSchema(id).then(function (schema) {
					res.status(200).send(schema);
				}).catch(function (err) {
					APIProvider.handleError(err, res, 404, 500);
				});
			});
		}
	}]);

	return SchemaAPI;
}(APIProvider);

exports = module.exports = SchemaAPI;