'use strict';

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var APIProvider = require('../APIProvider');

/**
 * API for interacting with the Codeblocks.
 * @public
 */

var CodeblocksAPI = function (_APIProvider) {
	_inherits(CodeblocksAPI, _APIProvider);

	function CodeblocksAPI() {
		_classCallCheck(this, CodeblocksAPI);

		return _possibleConstructorReturn(this, (CodeblocksAPI.__proto__ || Object.getPrototypeOf(CodeblocksAPI)).apply(this, arguments));
	}

	_createClass(CodeblocksAPI, null, [{
		key: 'bind',

		/**
      * Bind the paths to the controller methods.
      *
      * @param {CodeblocksController} controller - The main controller providing the
   *                                         implementation for this API.
      */
		value: function bind(controller) {
			var swagger = controller.ctx.swagger;

			// https://techweb.axway.com/jira/browse/RDPP-2086
			// FIXME: Swagger 2.0 only supports a limited subset of JSON schema draft 04.
			// See: https://github.com/OAI/OpenAPI-Specification/blob/master/versions/2.0.md#schema-object
			// This means that it is not possible to represent our flow schema in a Swagger 2.0
			// document because we use things like (oneOf, allOf).  The only choice here is to upgrade to
			// use Swagger 3.0, which allegedly supports them
			// See: https://blog.readme.io/an-example-filled-guide-to-swagger-3-2/#formatchanges
			// When fixed, replace with:
			var codeblockmetadata = {
				// $ref: 'http://axway.com/schemas/codeblockmetadata.json'
				type: 'object'
			};

			swagger.get('/codeblocks').operationId('getCodeblocks').tag('codeblock').summary('Gets an array of Codeblocks.').response(200, 'OK', {
				type: 'array',
				items: _extends({}, codeblockmetadata)
			}).response(500).action(function (req, res) {
				controller.getCodeblocks().then(function (codeblocks) {
					res.status(200).send(codeblocks);
				}).catch(function (err) {
					APIProvider.handleError(err, res, 500);
				});
			});

			swagger.get('/codeblocks/:name').operationId('getCodeblockByName').tag('codeblock').summary('Gets the specified codeblock.').parameter({ in: 'path', name: 'name', type: 'string', description: 'Name of codeblock to retrieve.' }).response(200, 'OK', codeblockmetadata).response(400).response(404).response(500).action(function (req, res) {
				var name = void 0;
				if (req.params.name) {
					name = decodeURIComponent(req.params.name);
				}
				controller.getCodeblock(name).then(function (APIs) {
					res.status(200).send(APIs);
				}).catch(function (err) {
					APIProvider.handleError(err, res, 400, 404, 500);
				});
			});
		}
	}]);

	return CodeblocksAPI;
}(APIProvider);

exports = module.exports = CodeblocksAPI;