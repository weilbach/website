/**
 * API that represents utilities for the whole Service Connectors lifecycle - generation, registration, execution etc.
 */
module.exports = {
  /**
   * Constructs the lifecycle methods for service connector
   * that are needed to hook the connector inside API Builder
   */
  getLifecycleMethods: require('./api-builder')
}
